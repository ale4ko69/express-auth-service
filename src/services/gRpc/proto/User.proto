syntax = 'proto3';

message Empty {}
message Subject {
    string _id = 1;
    string email = 2;
}
message Request {
    string options = 1;
    string methodName = 2;
}
message Response {
    string msg = 1;
}

service UserService {
    rpc fetch (Request) returns (Response) {}
}
